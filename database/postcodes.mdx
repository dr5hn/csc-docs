---
title: "Postcodes"
description: "844,248 postal codes across 125 countries with type classification, source tracking, and query examples"
icon: "mailbox"
---

The postcodes table contains **844,248 records** covering **125 countries**. Each record includes the postcode / ZIP code string, country, granularity type, source attribution, and locality name.

<Warning>
  This is informational postcode data, not an address verification or mail-deliverability service. A record confirms a code exists in the dataset — it does not confirm a delivery point, standardize an address, or guarantee mail will arrive.
</Warning>

## Coverage

| Stat | Value |
|---|---|
| Total postcodes | 844,248 |
| Countries covered | 125 |
| Granularity types | 5 (full, outward, sector, district, area) |

## Postcode Types

| Type | Description | Example |
|---|---|---|
| `full` | Complete postcode identifying a specific delivery point or small area | `90210` (US), `110001` (IN) |
| `outward` | First part of a UK-style postcode identifying the postal district | `SW1A` |
| `sector` | Intermediate granularity within a district | `SW1A 1` |
| `district` | Broadest level, covering an entire postal district | `SW1` |
| `area` | A UK-style postal area code (letters only, e.g. `SW`), or the general granularity used for countries without a full outward/sector/district hierarchy | `SW` (UK area), `1234` (Netherlands) |

## Query Examples

### Look up postcodes for a country

<CodeGroup>
```sql MySQL / PostgreSQL
SELECT code, locality_name, type
FROM postcodes
WHERE country_code = 'IN'
ORDER BY code
LIMIT 20;
```

```javascript Node.js
const response = await fetch('https://api.countrystatecity.in/v1/countries/IN/postcodes?limit=20', {
  headers: { 'X-CSCAPI-KEY': 'YOUR_API_KEY' }
});
const page = await response.json();
console.log(page.data);          // array of postcode records
console.log(page.pagination);    // { limit, next_cursor, has_more }
```

```python Python
import requests

response = requests.get(
  'https://api.countrystatecity.in/v1/countries/IN/postcodes',
  params={'limit': 20},
  headers={'X-CSCAPI-KEY': 'YOUR_API_KEY'}
)
page = response.json()
postcodes = page['data']
```
</CodeGroup>

<Tip>
  This lists postcodes without a plan restriction. Searching by code prefix or locality (`?q=`) requires a Supporter+ plan — see [Get Postcodes by Country](/api/endpoints/get-postcodes-by-country) for full query parameters, pagination, and tier-based field availability. For a single known code, [Get Postcode by Code](/api/endpoints/get-postcode-by-code) is free on every plan.
</Tip>

### Validate a postcode format

Use the `postal_code_regex` field on the countries table to validate a postcode before querying:

```sql
SELECT c.name, c.postal_code_format, c.postal_code_regex
FROM countries c
WHERE c.iso2 = 'GB';
-- postal_code_format: "@# #@@" or "@## #@@" etc.
-- postal_code_regex:  GIR[ ]?0AA|...
```

### Find postcodes by locality

```sql
SELECT code, country_code, type, locality_name
FROM postcodes
WHERE country_code = 'GB'
  AND locality_name ILIKE '%London%'
ORDER BY type, code;
```

### Count postcodes per country

```sql
SELECT country_code, COUNT(*) AS total, COUNT(DISTINCT type) AS types
FROM postcodes
GROUP BY country_code
ORDER BY total DESC
LIMIT 10;
```

## Availability by Format

The postcodes table is included in all SQL-based exports (MySQL, PostgreSQL, SQLite, SQL Server), the JSON export, and the CSV and YAML exports. It is **not** included in the GeoJSON export.

<Tip>
  Use the [Export Tool](https://export.countrystatecity.in) to download a postcodes-only dataset for specific countries, which is significantly smaller than the full dump.
</Tip>

## Related

<CardGroup cols={2}>
  <Card title="Get Postcode by Code" icon="location-dot" href="/api/endpoints/get-postcode-by-code">
    Exact lookup via the hosted API, free on every plan.
  </Card>
  <Card title="Get Postcodes by Country" icon="magnifying-glass" href="/api/endpoints/get-postcodes-by-country">
    Paginated listing and search via the hosted API. Requires Supporter+.
  </Card>
  <Card title="Database Schema" icon="sitemap" href="/database/schema">
    Full table definitions including the postcodes table structure.
  </Card>
  <Card title="Export Tool" icon="download" href="/tools/export-tool">
    Export postcodes data in your preferred format.
  </Card>
</CardGroup>
